<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Moten · IP Control Plane</title>
  <style>
    :root { --ink:#e8edf6; --muted:#8b98ad; --line:#263247; --panel:#121a29; --panel2:#182235; --bg:#0a101b; --accent:#74e0c0; --accent2:#9a8cff; --amber:#f4c66a; --red:#ff7f86; --blue:#79b8ff; --green:#73dfae; }
    * { box-sizing:border-box; } body { margin:0; min-height:100vh; background:var(--bg); color:var(--ink); font:14px/1.45 Inter,ui-sans-serif,system-ui,-apple-system,sans-serif; }
    button,input,select,textarea { font:inherit; } button { cursor:pointer; }
    .app { display:flex; min-height:100vh; } .rail { width:246px; flex:0 0 246px; padding:28px 18px; border-right:1px solid var(--line); background:#0d1522; }
    .brand { display:flex; align-items:center; gap:11px; margin:0 10px 34px; } .mark { width:32px; height:32px; border:1px solid #71e3c2; border-radius:10px; display:grid; place-items:center; color:var(--accent); font-weight:800; }
    .brand strong { display:block; letter-spacing:.08em; font-size:13px; } .brand span { color:var(--muted); font-size:10px; text-transform:uppercase; letter-spacing:.12em; }
    .nav-label { margin:22px 12px 8px; color:#58677e; text-transform:uppercase; letter-spacing:.12em; font-size:10px; } .nav-btn { width:100%; border:0; background:transparent; border-radius:9px; padding:11px 12px; color:var(--muted); text-align:left; display:flex; align-items:center; gap:11px; }
    .nav-btn:hover,.nav-btn.active { color:var(--ink); background:#1a273a; } .nav-btn.active { box-shadow:inset 2px 0 var(--accent); } .nav-icon { width:18px; text-align:center; color:var(--accent); }
    .rail-foot { margin:42px 10px 0; padding-top:18px; border-top:1px solid var(--line); color:var(--muted); font-size:11px; } .privacy { display:flex; align-items:center; gap:6px; color:var(--green); margin-bottom:8px; }
    main { width:min(1360px,100%); margin:0 auto; padding:28px 42px 60px; } .topbar { display:flex; justify-content:space-between; align-items:flex-start; gap:20px; margin-bottom:34px; }
    .eyebrow { color:var(--accent); text-transform:uppercase; letter-spacing:.14em; font-size:10px; font-weight:700; } h1 { font-size:clamp(26px,3vw,38px); line-height:1.1; margin:7px 0 8px; letter-spacing:-.04em; } h2 { font-size:19px; margin:0; letter-spacing:-.02em; } h3 { font-size:14px; margin:0 0 7px; } p { margin:0; color:var(--muted); }
    .top-actions { display:flex; align-items:center; gap:10px; } .operator { border:1px solid var(--line); padding:8px 12px; border-radius:8px; color:var(--muted); font-size:12px; }
    .btn { border:1px solid var(--line); background:var(--panel2); color:var(--ink); padding:10px 14px; border-radius:8px; font-weight:650; } .btn:hover { border-color:#4a617a; background:#20304a; } .btn.primary { background:var(--accent); color:#0a1719; border-color:var(--accent); } .btn.primary:hover { background:#9bf0d5; } .btn.small { font-size:12px; padding:7px 10px; }
    .grid { display:grid; gap:14px; } .stats { grid-template-columns:repeat(4,minmax(0,1fr)); margin-bottom:18px; } .panel { background:var(--panel); border:1px solid var(--line); border-radius:12px; }
    .stat { padding:18px; position:relative; overflow:hidden; } .stat:after { content:""; position:absolute; width:90px; height:90px; right:-30px; bottom:-45px; border-radius:50%; border:1px solid #263a4a; }
    .stat-label { color:var(--muted); font-size:11px; text-transform:uppercase; letter-spacing:.08em; } .stat-value { font-size:28px; margin:8px 0 2px; letter-spacing:-.05em; } .stat-note { color:var(--accent); font-size:11px; }
    .dashboard-grid { grid-template-columns:1.4fr .9fr; } .section { padding:22px; } .section-head { display:flex; justify-content:space-between; align-items:flex-start; gap:12px; margin-bottom:18px; } .section-head p { font-size:12px; margin-top:4px; }
    .control-banner { display:flex; gap:14px; align-items:flex-start; padding:15px; border-radius:9px; background:#172d2c; border:1px solid #245852; margin-bottom:18px; } .control-banner .seal { color:var(--accent); font-size:20px; } .control-banner strong { font-size:13px; } .control-banner p { font-size:12px; margin-top:3px; }
    .timeline { display:grid; gap:0; } .timeline-row { display:grid; grid-template-columns:90px 1fr; gap:16px; padding:13px 0; border-bottom:1px solid #202c3d; } .timeline-row:last-child { border-bottom:0; } .timeline-date { color:var(--accent); font-size:11px; font-weight:700; } .timeline-row strong { font-size:13px; } .timeline-row span { display:block; color:var(--muted); font-size:12px; margin-top:3px; }
    .control-list { display:grid; gap:10px; } .control-item { padding:13px; background:var(--panel2); border-radius:9px; border:1px solid #223149; } .control-item strong { display:flex; align-items:center; gap:7px; font-size:12px; } .control-item p { font-size:11px; margin-top:5px; } .dot { width:7px; height:7px; display:inline-block; border-radius:50%; background:var(--green); } .dot.amber { background:var(--amber); } .dot.red { background:var(--red); }
    .view { display:none; } .view.active { display:block; } .table-wrap { overflow:auto; } table { width:100%; border-collapse:collapse; min-width:700px; } th { color:#687890; font-size:10px; text-transform:uppercase; letter-spacing:.1em; text-align:left; font-weight:700; padding:11px 12px; border-bottom:1px solid var(--line); } td { padding:14px 12px; border-bottom:1px solid #202c3d; vertical-align:middle; } tr:last-child td { border-bottom:0; } td strong { font-size:13px; } td small { display:block; color:var(--muted); margin-top:3px; }
    .id { color:var(--accent); font:11px ui-monospace,SFMono-Regular,monospace; } .pill { display:inline-flex; align-items:center; border:1px solid #33445d; border-radius:20px; padding:4px 8px; color:var(--muted); font-size:10px; white-space:nowrap; } .pill.green { color:var(--green); border-color:#2c634f; background:#102a26; } .pill.amber { color:var(--amber); border-color:#66502a; background:#282116; } .pill.red { color:var(--red); border-color:#63343d; background:#2a171e; } .pill.blue { color:var(--blue); border-color:#304e70; background:#142336; }
    .cards { grid-template-columns:repeat(3,minmax(0,1fr)); } .candidate { padding:18px; min-height:220px; display:flex; flex-direction:column; } .candidate-head { display:flex; justify-content:space-between; gap:10px; } .candidate h3 { font-size:15px; line-height:1.25; } .candidate .mechanism { color:#c0cadd; font-size:12px; margin:15px 0; flex:1; } .candidate-foot { display:flex; align-items:center; justify-content:space-between; border-top:1px solid #253247; padding-top:13px; } .candidate-foot small { color:var(--muted); }
    .filters { display:flex; gap:8px; flex-wrap:wrap; margin-bottom:16px; } .filter { border:1px solid var(--line); color:var(--muted); background:transparent; padding:7px 11px; border-radius:20px; font-size:11px; } .filter.active,.filter:hover { color:var(--ink); border-color:var(--accent); }
    .two-col { grid-template-columns:1fr 1fr; } .form-grid { display:grid; grid-template-columns:1fr 1fr; gap:13px; } .field { display:grid; gap:6px; } .field.full { grid-column:1 / -1; } label { color:#b5c0d2; font-size:11px; font-weight:650; }
    input,select,textarea { width:100%; color:var(--ink); background:#0d1522; border:1px solid #2b3a51; border-radius:7px; padding:10px; outline:none; } input:focus,select:focus,textarea:focus { border-color:var(--accent); box-shadow:0 0 0 2px #74e0c01f; } textarea { min-height:85px; resize:vertical; } .check { display:flex; align-items:center; gap:8px; color:var(--muted); font-size:12px; } .check input { width:auto; accent-color:var(--accent); }
    .decision { padding:17px; border-radius:9px; margin-top:16px; border:1px solid var(--line); } .decision.green { border-color:#2c634f; background:#102a26; } .decision.amber { border-color:#66502a; background:#282116; } .decision.red { border-color:#63343d; background:#2a171e; } .decision strong { font-size:15px; } .decision p { font-size:12px; margin-top:4px; } .decision-meta { margin-top:12px; display:flex; gap:7px; flex-wrap:wrap; }
    .deadline-grid { grid-template-columns:repeat(4,minmax(0,1fr)); } .deadline { padding:17px; border-left:3px solid var(--accent2); } .deadline.soon { border-left-color:var(--amber); } .deadline.late { border-left-color:var(--red); } .deadline .month { color:var(--accent2); text-transform:uppercase; letter-spacing:.1em; font-size:10px; font-weight:700; } .deadline.soon .month { color:var(--amber); } .deadline.late .month { color:var(--red); } .deadline .date { font-size:22px; margin:10px 0 3px; letter-spacing:-.04em; } .deadline small { color:var(--muted); }
    .workbench { grid-template-columns:1.1fr .9fr; } .queue-row { display:flex; gap:12px; align-items:flex-start; padding:14px 0; border-bottom:1px solid #202c3d; } .queue-row:last-child { border-bottom:0; } .queue-icon { width:27px; height:27px; border-radius:7px; display:grid; place-items:center; background:#24344a; color:var(--blue); } .queue-row strong { font-size:13px; } .queue-row p { font-size:11px; margin-top:3px; }
    .modal { position:fixed; inset:0; background:#050910b8; display:none; align-items:center; justify-content:center; padding:20px; z-index:5; backdrop-filter:blur(6px); } .modal.show { display:flex; } .modal-box { background:#111b2b; border:1px solid #35455d; border-radius:13px; width:min(720px,100%); max-height:90vh; overflow:auto; box-shadow:0 24px 70px #0008; } .modal-head { display:flex; justify-content:space-between; align-items:flex-start; padding:22px 24px; border-bottom:1px solid var(--line); } .modal-head p { font-size:12px; margin-top:4px; } .close { border:0; background:transparent; color:var(--muted); font-size:23px; } .modal-body { padding:24px; } .modal-actions { display:flex; justify-content:flex-end; gap:9px; margin-top:21px; }
    .detail-block { padding:14px 0; border-bottom:1px solid #273348; } .detail-block:last-child { border-bottom:0; } .detail-block h4 { color:var(--muted); text-transform:uppercase; letter-spacing:.1em; font-size:10px; margin:0 0 8px; } .detail-block p { color:#c5cfde; font-size:13px; } .contributor { display:flex; align-items:center; justify-content:space-between; gap:12px; padding:9px 0; } .contributor + .contributor { border-top:1px solid #253247; } .contributor span { color:var(--muted); font-size:11px; }
    .toast { position:fixed; right:22px; bottom:22px; z-index:10; background:#e8fdf5; color:#0b2720; padding:12px 16px; border-radius:8px; box-shadow:0 10px 30px #0005; display:none; font-weight:650; } .toast.show { display:block; animation:rise .2s ease-out; } @keyframes rise { from { transform:translateY(8px); opacity:0; } }
    @media (max-width:1050px) { .rail { width:210px; flex-basis:210px; } main { padding:25px; } .cards { grid-template-columns:repeat(2,1fr); } .deadline-grid { grid-template-columns:repeat(2,1fr); } }
    @media (max-width:760px) { .app { display:block; } .rail { width:100%; padding:15px; border-right:0; border-bottom:1px solid var(--line); } .brand { margin-bottom:15px; } .nav-label,.rail-foot { display:none; } .nav { display:flex; overflow:auto; gap:5px; } .nav-btn { width:auto; white-space:nowrap; } .topbar { display:block; } .top-actions { margin-top:16px; } .stats,.dashboard-grid,.workbench,.two-col { grid-template-columns:1fr; } main { padding:22px 15px 45px; } .cards { grid-template-columns:1fr; } .form-grid { grid-template-columns:1fr; } .field.full { grid-column:auto; } }
  </style>
</head>
<body>
  <div class="app">
    <aside class="rail">
      <div class="brand"><div class="mark">M</div><div><strong>MOTEN</strong><span>control plane</span></div></div>
      <nav class="nav" aria-label="Primary navigation">
        <div class="nav-label">Portfolio</div>
        <button class="nav-btn active" data-view="overview"><span class="nav-icon">◈</span>Command center</button>
        <button class="nav-btn" data-view="registry"><span class="nav-icon">⌁</span>Research registry</button>
        <button class="nav-btn" data-view="inventions"><span class="nav-icon">◇</span>Invention ledger</button>
        <div class="nav-label">Controls</div>
        <button class="nav-btn" data-view="disclosures"><span class="nav-icon">⊘</span>Disclosure firewall <span class="pill red">2</span></button>
        <button class="nav-btn" data-view="calendar"><span class="nav-icon">◷</span>Filing calendar</button>
        <button class="nav-btn" data-view="counsel"><span class="nav-icon">▣</span>Counsel workbench</button>
      </nav>
      <div class="rail-foot"><div class="privacy"><span class="dot"></span> Restricted workspace</div><div>Provenance is not a filing date.</div><div style="margin-top:5px">Phase 1 · Days 0–30</div></div>
    </aside>
    <main>
      <div class="topbar"><div><div class="eyebrow" id="view-eyebrow">Portfolio / Phase 1</div><h1 id="view-title">Command center</h1><p id="view-subtitle">Remember what matters before it becomes irreversible.</p></div><div class="top-actions"><span class="operator">● Andre · IP steward</span><button class="btn primary" onclick="openModal('invention')">＋ Capture invention</button></div></div>
      <section class="view active" id="view-overview">
        <div class="control-banner"><div class="seal">✦</div><div><strong>Control-plane constitution is active</strong><p>Dates carry legal meaning. Versions never disappear. Unknown authority means hold. Humans own conception.</p></div><span class="pill green" style="margin-left:auto">ENFORCED</span></div>
        <div class="grid stats"><div class="panel stat"><div class="stat-label">Tracked candidates</div><div class="stat-value" id="stat-candidates">12</div><div class="stat-note">6 Three-Zone · 6 Treasure</div></div><div class="panel stat"><div class="stat-label">Research instruments</div><div class="stat-value" id="stat-questions">3</div><div class="stat-note">exact wording preserved</div></div><div class="panel stat"><div class="stat-label">Disclosure holds</div><div class="stat-value" id="stat-holds">2</div><div class="stat-note">file-first or counsel review</div></div><div class="panel stat"><div class="stat-label">Next hard gate</div><div class="stat-value">T+6 mo</div><div class="stat-note">INV-2026-000017 · 02 Mar 2027</div></div></div>
        <div class="grid dashboard-grid"><div class="panel section"><div class="section-head"><div><h2>Irreversible-risk queue</h2><p>Events that need an accountable human next.</p></div><button class="btn small" onclick="showView('calendar')">Open calendar →</button></div><div class="timeline"><div class="timeline-row"><div class="timeline-date">TODAY</div><div><strong>Disclosure preflight · partner-deck-03</strong><span><span class="pill red">HOLD-FILE-FIRST</span> linked to unfiled TZ-02 mechanism · IP steward</span></div></div><div class="timeline-row"><div class="timeline-date">IN 7 DAYS</div><div><strong>Counsel triage · INV-2026-000017</strong><span>Inventor list, ownership chain, and public disclosure history · Counsel</span></div></div><div class="timeline-row"><div class="timeline-date">02 MAR 27</div><div><strong>Six-month portfolio review</strong><span>Support map, new embodiments, secret split, and foreign strategy · Founder + counsel</span></div></div><div class="timeline-row"><div class="timeline-date">02 SEP 27</div><div><strong>12-month decision</strong><span>Nonprovisional / PCT / foreign / abandon / retain as secret · Hard deadline</span></div></div></div></div>
          <div class="panel section"><div class="section-head"><div><h2>Control health</h2><p>Phase 1 acceptance surface.</p></div><span class="pill green">6 / 6 online</span></div><div class="control-list"><div class="control-item"><strong><span class="dot"></span>Immutable versioning</strong><p>Corrections create compensating records; originals remain visible.</p></div><div class="control-item"><strong><span class="dot"></span>Human contribution</strong><p>AI is captured as a tool, never as an inventor or authority.</p></div><div class="control-item"><strong><span class="dot"></span>Disclosure preflight</strong><p>Unfiled mechanism links default to a file-first hold.</p></div><div class="control-item"><strong><span class="dot amber"></span>Runtime planes</strong><p>Rights, sockets, media, and settlement are intentionally deferred.</p></div></div></div>
        </div>
      </section>
      <section class="view" id="view-registry"><div class="section-head"><div><h2>Research registry</h2><p>Questions are permanent instruments, not disposable prompts.</p></div><button class="btn primary" onclick="openModal('question')">＋ New question version</button></div><div class="panel section table-wrap"><table><thead><tr><th>Instrument</th><th>Exact wording</th><th>Intent / geography</th><th>First use</th><th>Evidence</th><th></th></tr></thead><tbody id="question-rows"></tbody></table></div></section>
      <section class="view" id="view-inventions"><div class="section-head"><div><h2>Invention ledger</h2><p>Mechanisms first. Slogans do not advance to counsel.</p></div><button class="btn primary" onclick="openModal('invention')">＋ Capture candidate</button></div><div class="filters" id="family-filters"><button class="filter active" data-family="all">All candidates</button><button class="filter" data-family="Three-Zone">Three-Zone</button><button class="filter" data-family="Treasure">Treasure</button><button class="filter" data-family="Counsel triage">Counsel triage</button></div><div class="grid cards" id="candidate-cards"></div></section>
      <section class="view" id="view-disclosures"><div class="section-head"><div><h2>Disclosure firewall</h2><p>Public release is privileged. Every artifact gets a hash, audience, and decision.</p></div><span class="pill red">2 active holds</span></div><div class="grid two-col"><div class="panel section"><h3>Run IP preflight</h3><p style="font-size:12px;margin-bottom:18px">The default is hold-file-first when an unfiled mechanism is linked.</p><div class="form-grid"><div class="field full"><label for="artifact-name">Artifact name / version</label><input id="artifact-name" placeholder="partner-deck-03 · v1.2"></div><div class="field"><label for="channel">Channel</label><select id="channel"><option>partner deck</option><option>public website</option><option>conference talk</option><option>external AI prompt</option><option>vendor brief</option></select></div><div class="field"><label for="classification">Classification</label><select id="classification"><option>partner-confidential</option><option>internal</option><option>NDA</option><option>public</option><option>regulated / sensitive</option></select></div><div class="field"><label for="linked-invention">Linked invention</label><select id="linked-invention"><option value="">No linked invention</option></select></div><div class="field"><label for="nda">NDA / contract status</label><select id="nda"><option value="verified">Verified · NDA-2026-0004</option><option value="missing">Missing / unknown</option></select></div></div><div class="modal-actions"><button class="btn primary" onclick="runPreflight()">Evaluate artifact →</button></div><div id="decision-output"></div></div><div class="panel section"><div class="section-head"><div><h3>Recent decisions</h3><p>Hashes and approved release versions remain linked.</p></div></div><div id="disclosure-list"></div></div></div></section>
      <section class="view" id="view-calendar"><div class="section-head"><div><h2>Filing calendar</h2><p>Clock starts from the verified USPTO filing date, never from a draft.</p></div><span class="pill amber">No silent snooze</span></div><div class="panel section" style="margin-bottom:18px"><div class="section-head"><div><h3>INV-2026-000017 · TZ-02 demand-guided rights acquisition</h3><p><span class="pill blue">USPTO filing date</span> 02 Sep 2026 · provisional receipt verified · APP-US-2026-000009</p></div><span class="pill green">FILED</span></div><div class="grid deadline-grid" id="deadline-grid"></div></div><div class="panel section"><h3>Calendar rules</h3><div class="grid two-col" style="margin-top:14px"><div class="control-item"><strong><span class="dot"></span>Verified date only</strong><p>A deadline cannot be created from a draft or private timestamp.</p></div><div class="control-item"><strong><span class="dot"></span>Escalation chain</strong><p>Owner → IP steward → founder → counsel. Acknowledgments are recorded.</p></div></div></div></section>
      <section class="view" id="view-counsel"><div class="section-head"><div><h2>Counsel workbench</h2><p>Evidence package readiness without declaring patentability.</p></div><button class="btn primary" onclick="showToast('Export queued','Counsel-ready package will include hashes, versions, and human contributions.')">Export evidence package</button></div><div class="grid workbench"><div class="panel section"><div class="section-head"><div><h3>Review queue</h3><p>Human decisions required before irreversible actions.</p></div><span class="pill amber">3 open</span></div><div class="queue-row"><div class="queue-icon">✦</div><div><strong>Inventorship and ownership triage</strong><p>INV-2026-000017 · contribution matrix has 2 human attestations.</p></div><span class="pill amber">7 days</span></div><div class="queue-row"><div class="queue-icon">⊘</div><div><strong>Partner deck preflight</strong><p>DISC-2026-000031 · ranking weights must be removed or filed first.</p></div><span class="pill red">hold</span></div><div class="queue-row"><div class="queue-icon">◇</div><div><strong>Embodiment gap</strong><p>TZ-04 rights-bound media object · add low-resource and failure-tolerant variants.</p></div><span class="pill blue">draft</span></div></div><div class="panel section"><h3>Phase 1 boundary</h3><p style="font-size:12px;margin:5px 0 18px">Built now, deliberately deferred later.</p><div class="control-list"><div class="control-item"><strong><span class="dot"></span>Built in this MVP</strong><p>IDs, question versions, candidate ledger, contribution records, preflight, calendar, counsel queue.</p></div><div class="control-item"><strong><span class="dot amber"></span>Days 31–90</strong><p>Hash manifests, append-only event service, access policy, AI gateway, support matrix, rights object.</p></div><div class="control-item"><strong><span class="dot amber"></span>Months 3–12</strong><p>Runtime leases, sockets, ingest, discovery, measurement, settlement, WORM storage, drills.</p></div></div></div></div></section>
    </main>
  </div>
  <div class="toast" id="toast"></div><div class="modal" id="modal"><div class="modal-box" id="modal-box"></div></div>
  <script>
    const seed = {
      questions:[
        {id:'RQ-2026-000001',version:'v1.0',title:'Home-zone willingness',wording:'Would you pay $5 per month to watch participating sports in your home zone?',intent:'Exploratory',geography:'Kansas City home zone',firstUse:'02 Sep 2026',evidence:4},
        {id:'RQ-2026-000002',version:'v1.0',title:'Relationship relevance',wording:'Which relationship makes this event personally relevant to you?',intent:'Stable longitudinal',geography:'US · relationship cohort',firstUse:'03 Sep 2026',evidence:7},
        {id:'RQ-2027-000044',version:'v1.0',title:'Authorized access window',wording:'Would you pay $7 per month for authorized home-zone access including replay?',intent:'New instrument',geography:'US · pilot market',firstUse:'11 Apr 2027',evidence:2}
      ],
      inventions:[
        {id:'INV-2026-000017',family:'Three-Zone',code:'TZ-02',title:'Demand-guided rights acquisition engine',status:'Filed',posture:'Hybrid',updated:'02 Sep 2026',gate:'6-month portfolio review',mechanism:'Joins stable longitudinal demand observations to versioned property availability, rights expiry, production readiness, and economics, then emits a ranked opportunity set with an evidence trace and human approval gate.',evidence:['RQ-2026-000001','OBS-2026-000184'],contributors:2},
        {id:'INV-2026-000018',family:'Three-Zone',code:'TZ-01',title:'Rights-aware distribution router',status:'Invention candidate',posture:'Patent candidate',updated:'04 Sep 2026',gate:'Add failure-tolerant embodiment',mechanism:'Joins household relationship, service area, package, rights, territory, device, and destination to route only to an authorized experience.',evidence:['RQ-2026-000002'],contributors:1},
        {id:'INV-2026-000019',family:'Three-Zone',code:'TZ-03',title:'Adaptive broadcast orchestration',status:'Hypothesis',posture:'Hold for counsel',updated:'04 Sep 2026',gate:'Capture first technical effect',mechanism:'Selects ingest, certified producer, or full-service broadcast from rights, feed quality, local capability, cost, and reliability.',evidence:[],contributors:1},
        {id:'INV-2026-000020',family:'Three-Zone',code:'TZ-04',title:'Rights-bound media object',status:'Mechanism',posture:'Patent candidate',updated:'05 Sep 2026',gate:'Support map',mechanism:'Carries territory, window, replay, clips, ad, master, distributor, expiration, and authorization data with every media derivative.',evidence:['OBS-2026-000184'],contributors:2},
        {id:'INV-2026-000021',family:'Three-Zone',code:'TZ-05',title:'Auditable multi-property settlement',status:'Observation',posture:'Hold for counsel',updated:'05 Sep 2026',gate:'Define correction event',mechanism:'Computes base, performance, advertising, premium, syndication, cost, and platform economics from immutable measurement facts and formula versions.',evidence:[],contributors:1},
        {id:'INV-2026-000022',family:'Three-Zone',code:'TZ-06',title:'Relationship-based sports discovery graph',status:'Hypothesis',posture:'Hold for counsel',updated:'05 Sep 2026',gate:'Privacy boundary review',mechanism:'Navigates from family, school, hometown, alumni, conference, affiliate, or team-system relation to rights-aware availability.',evidence:['RQ-2026-000002'],contributors:1},
        {id:'INV-2026-000023',family:'Treasure',code:'TR-01',title:'Reward / evidence separation',status:'Mechanism',posture:'Patent candidate',updated:'05 Sep 2026',gate:'Add reversal test',mechanism:'Rewards a bounded activity without changing the underlying evidence history through independent ledgers and signed event references.',evidence:[],contributors:1},
        {id:'INV-2026-000024',family:'Treasure',code:'TR-02',title:'Capped score impact with persistent evidence',status:'Hypothesis',posture:'Hold for counsel',updated:'05 Sep 2026',gate:'Replay correctness test',mechanism:'Limits the effect of one event on a score while continuing to accumulate evidence for later context.',evidence:[],contributors:1},
        {id:'INV-2026-000025',family:'Treasure',code:'TR-03',title:'Multi-activity consistency history',status:'Observation',posture:'Hold for counsel',updated:'05 Sep 2026',gate:'Define transparent aggregation',mechanism:'Represents consistency across activities without collapsing different evidence types into one unexplainable number.',evidence:[],contributors:1},
        {id:'INV-2026-000026',family:'Treasure',code:'TR-04',title:'Participant-selected evidence presentation',status:'Hypothesis',posture:'Patent candidate',updated:'05 Sep 2026',gate:'Selective disclosure test',mechanism:'Lets a participant present selected evidence without exposing full private history through consented views, redaction, provenance, and revocation.',evidence:[],contributors:1},
        {id:'INV-2026-000027',family:'Treasure',code:'TR-05',title:'AI-assisted control architecture',status:'Mechanism',posture:'Hybrid',updated:'05 Sep 2026',gate:'Leakage red-team',mechanism:'Uses AI for explanation and organization while preserving human and policy authority with an action allowlist and hashed outputs.',evidence:[],contributors:1},
        {id:'INV-2026-000028',family:'Treasure',code:'TR-06',title:'Anonymous-to-authorized identity transition',status:'Hypothesis',posture:'Hold for counsel',updated:'05 Sep 2026',gate:'Consent proof',mechanism:'Starts with anonymous aggregate intelligence and links to identity only after an authorized purpose gate.',evidence:[],contributors:1}
      ],
      disclosures:[
        {id:'DISC-2026-000031',artifact:'partner-deck-03 · v1.2',channel:'partner deck',decision:'HOLD-FILE-FIRST',classification:'partner-confidential',linked:'INV-2026-000017',hash:'sha256: 4b7c…a92e',date:'05 Sep 2026'},
        {id:'DISC-2026-000030',artifact:'research-methodology · v1.0',channel:'counsel packet',decision:'AMBER · NDA VERIFIED',classification:'NDA',linked:'INV-2026-000018',hash:'sha256: b91e…0cd4',date:'04 Sep 2026'}
      ],
      contributions:[
        {person:'Andre',role:'Relationship / mechanism',detail:'Recognized the demand-to-rights interaction and selected the event-scoped approval gate.',date:'02 Sep 2026'},
        {person:'Andre',role:'Implementation / verification',detail:'Defined the evidence trace and ranked-opportunity output contract.',date:'02 Sep 2026'}
      ]
    };
    let state=JSON.parse(localStorage.getItem('moten-phase1'))||seed, activeFamily='all';
    const titles={overview:['Portfolio / Phase 1','Command center','Remember what matters before it becomes irreversible.'],registry:['Signal plane','Research registry','Questions are instruments. Exact wording is evidence.'],inventions:['Invention plane','Invention ledger','Mechanisms first. Slogans do not advance to counsel.'],disclosures:['Decision plane','Disclosure firewall','No public release should outrun the IP decision.'],calendar:['Decision plane','Filing calendar','Make the 12-month clock impossible to miss.'],counsel:['Counsel + governance','Counsel workbench','The system proves process. Counsel decides patentability.']};
    const $=id=>document.getElementById(id), esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
    function persist(){localStorage.setItem('moten-phase1',JSON.stringify(state));} function showToast(title,message=''){const toast=$('toast');toast.textContent=message?`${title} · ${message}`:title;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3600);}
    function statusClass(status){if(/Filed|Allowed|verified/i.test(status))return'green';if(/Hold|Red|Prohibited/i.test(status))return'red';if(/Amber|Review|Hypothesis/i.test(status))return'amber';return'blue';}
    function showView(view){document.querySelectorAll('.view').forEach(el=>el.classList.toggle('active',el.id===`view-${view}`));document.querySelectorAll('.nav-btn').forEach(el=>el.classList.toggle('active',el.dataset.view===view));const copy=titles[view];$('view-eyebrow').textContent=copy[0];$('view-title').textContent=copy[1];$('view-subtitle').textContent=copy[2];if(view==='registry')renderQuestions();if(view==='inventions')renderCandidates();if(view==='disclosures')renderDisclosures();if(view==='calendar')renderCalendar();}
    document.querySelectorAll('.nav-btn').forEach(button=>button.addEventListener('click',()=>showView(button.dataset.view))); document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{activeFamily=button.dataset.family;document.querySelectorAll('.filter').forEach(item=>item.classList.toggle('active',item===button));renderCandidates();}));
    function renderQuestions(){$('question-rows').innerHTML=state.questions.map(q=>`<tr><td><span class="id">${esc(q.id)}</span><small>${esc(q.title)} · ${esc(q.version)}</small></td><td style="max-width:370px"><strong>${esc(q.wording)}</strong></td><td><span class="pill blue">${esc(q.intent)}</span><small>${esc(q.geography)}</small></td><td>${esc(q.firstUse)}<small>internal provenance date</small></td><td>${q.evidence} linked artifacts</td><td><button class="btn small" onclick="openModal('question','${esc(q.id)}')">Version</button></td></tr>`).join('');$('stat-questions').textContent=new Set(state.questions.map(q=>q.id)).size;}
    function renderCandidates(){const list=activeFamily==='all'?state.inventions:activeFamily==='Counsel triage'?state.inventions.filter(item=>/Hold|Hypothesis/.test(item.status)):state.inventions.filter(item=>item.family===activeFamily);$('candidate-cards').innerHTML=list.map(item=>`<article class="panel candidate"><div class="candidate-head"><div><span class="id">${esc(item.id)}</span><h3>${esc(item.code)} · ${esc(item.title)}</h3></div><span class="pill ${statusClass(item.status)}">${esc(item.status)}</span></div><div class="mechanism">${esc(item.mechanism)}</div><div class="candidate-foot"><small>${esc(item.posture)} · ${item.contributors} human record${item.contributors===1?'':'s'}</small><button class="btn small" onclick="openModal('detail','${esc(item.id)}')">Inspect →</button></div></article>`).join('');$('stat-candidates').textContent=state.inventions.length;}
    function renderDisclosures(){$('linked-invention').innerHTML=`<option value="">No linked invention</option>${state.inventions.map(item=>`<option value="${esc(item.id)}">${esc(item.id)} · ${esc(item.code)} · ${esc(item.title)}</option>`).join('')}`;$('disclosure-list').innerHTML=state.disclosures.map(item=>`<div class="queue-row"><div class="queue-icon">${item.decision.startsWith('HOLD')?'⊘':'✓'}</div><div><strong>${esc(item.artifact)}</strong><p>${esc(item.channel)} · ${esc(item.linked||'no IP links')} · ${esc(item.hash)}</p></div><span class="pill ${item.decision.startsWith('HOLD')?'red':'amber'}">${esc(item.decision)}</span></div>`).join('');$('stat-holds').textContent=state.disclosures.filter(item=>item.decision.startsWith('HOLD')).length;}
    function addMonths(date,months){const result=new Date(`${date}T12:00:00Z`);result.setUTCMonth(result.getUTCMonth()+months);return result;} function fmt(date){return date.toLocaleDateString('en-US',{month:'short',day:'2-digit',year:'numeric',timeZone:'UTC'});}
    function renderCalendar(){const items=[[6,'Portfolio review','Founder + counsel'],[9,'Nonprovisional / PCT strategy','Counsel'],[10,'Final technical package','Founder + counsel'],[11,'Executive escalation','Founder']];$('deadline-grid').innerHTML=items.map(([months,label,owner])=>`<div class="panel deadline ${months>=10?'soon':''}"><div class="month">T+${months} months</div><div class="date">${fmt(addMonths('2026-09-02',months))}</div><strong>${label}</strong><small>${owner}<br>acknowledgment required</small></div>`).join('');}
    function openModal(kind,id=''){const modal=$('modal');modal.classList.add('show');if(kind==='question'){const current=id?state.questions.find(item=>item.id===id):null;$('modal-box').innerHTML=`<div class="modal-head"><div><h2>${current?'Create immutable question version':'Create research instrument'}</h2><p>${current?esc(current.id)+' · original remains preserved':'Exact wording and response contract are required.'}</p></div><button class="close" onclick="closeModal()">×</button></div><div class="modal-body"><div class="form-grid"><div class="field"><label>Question title</label><input id="q-title" value="${esc(current?current.title:'')}" placeholder="Home-zone willingness"></div><div class="field"><label>Intent</label><select id="q-intent"><option>Exploratory</option><option>Stable longitudinal</option><option>Controlled revision</option></select></div><div class="field full"><label>Exact wording · punctuation and instructions preserved</label><textarea id="q-wording" placeholder="Type the question exactly as presented">${esc(current?current.wording:'')}</textarea></div><div class="field"><label>Geography / population</label><input id="q-geo" value="${esc(current?current.geography:'')}" placeholder="US · pilot market"></div><div class="field"><label>Response contract</label><input id="q-contract" placeholder="yes / no / unsure · randomized"></div></div><div class="modal-actions"><button class="btn" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="saveQuestion('${esc(id)}')">Commit immutable version</button></div></div>`;}else if(kind==='invention'){$('modal-box').innerHTML=`<div class="modal-head"><div><h2>Capture mechanism hypothesis</h2><p>Business descriptions do not advance. Describe the machinery.</p></div><button class="close" onclick="closeModal()">×</button></div><div class="modal-body"><div class="form-grid"><div class="field"><label>Project / family</label><select id="i-family"><option>Three-Zone</option><option>Treasure</option></select></div><div class="field"><label>Candidate code</label><input id="i-code" placeholder="TZ-07"></div><div class="field full"><label>Title</label><input id="i-title" placeholder="Mechanism name, not a slogan"></div><div class="field full"><label>Problem · inputs → transformation → outputs</label><textarea id="i-mechanism" placeholder="What fails, what enters the system, what changes, and what comes out?"></textarea></div><div class="field"><label>Technical effect</label><input id="i-effect" placeholder="reduces stale-rights errors"></div><div class="field"><label>IP posture</label><select id="i-posture"><option>Hold for counsel</option><option>Patent candidate</option><option>Trade-secret candidate</option><option>Hybrid</option></select></div><div class="field full"><label>First human conception / evidence</label><input id="i-contribution" placeholder="Andre · selected relationship · note-2026-09-05"></div></div><div class="modal-actions"><button class="btn" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="saveInvention()">Create INV record</button></div></div>`;}else if(kind==='detail'){const item=state.inventions.find(candidate=>candidate.id===id);$('modal-box').innerHTML=`<div class="modal-head"><div><span class="id">${esc(item.id)}</span><h2>${esc(item.title)}</h2><p>${esc(item.code)} · ${esc(item.family)} · ${esc(item.status)}</p></div><button class="close" onclick="closeModal()">×</button></div><div class="modal-body"><div class="detail-block"><h4>Mechanism</h4><p>${esc(item.mechanism)}</p></div><div class="detail-block"><h4>Technical context and effect</h4><p>This candidate is recorded as a testable system hypothesis. Technical effect, failure behavior, security boundary, and alternatives must be evidenced before counsel review.</p></div><div class="detail-block"><h4>Evidence links</h4><p>${item.evidence.length?item.evidence.map(link=>`<span class="pill blue">${esc(link)}</span>`).join(' '):'<span class="pill amber">No evidence linked yet</span>'}</p></div><div class="detail-block"><h4>Human contribution record</h4>${state.contributions.map(contribution=>`<div class="contributor"><div><strong>${esc(contribution.person)}</strong><span>${esc(contribution.role)}</span></div><span>${esc(contribution.date)}</span></div>`).join('')}</div><div class="detail-block"><h4>Next evidence gate</h4><p><span class="pill amber">${esc(item.gate)}</span> · AI assistance remains tool-only and non-canonical.</p></div><div class="modal-actions"><button class="btn" onclick="openContribution('${esc(item.id)}')">＋ Add contribution</button><button class="btn primary" onclick="closeModal()">Close record</button></div></div>`;}}
    function openContribution(id){$('modal-box').querySelector('.modal-body').insertAdjacentHTML('beforeend',`<div class="detail-block" id="contribution-form"><h4>New human contribution</h4><div class="form-grid"><div class="field"><label>Person</label><input id="c-person" value="Andre"></div><div class="field"><label>Contribution class</label><select id="c-role"><option>Problem recognition</option><option>Constraint definition</option><option>Relationship / mechanism</option><option>Selection</option><option>Implementation</option><option>Verification</option></select></div><div class="field full"><label>Plain-language contribution</label><textarea id="c-detail" placeholder="What did the human accept, change, reject, or add?"></textarea></div></div><div class="modal-actions"><button class="btn primary" onclick="saveContribution('${esc(id)}')">Attest contribution</button></div></div>`);}
    function closeModal(){$('modal').classList.remove('show');} $('modal').addEventListener('click',event=>{if(event.target===$('modal'))closeModal();});
    function saveQuestion(existingId){const wording=$('q-wording').value.trim();if(!wording)return showToast('Exact wording required');const existing=existingId?state.questions.filter(item=>item.id===existingId):[],id=existingId||`RQ-2026-${String(state.questions.length+1).padStart(6,'0')}`;state.questions.push({id,version:`v1.${existing.length}`,title:$('q-title').value.trim()||'Untitled instrument',wording,intent:$('q-intent').value,geography:$('q-geo').value.trim()||'Unspecified',firstUse:'05 Sep 2026',evidence:0});persist();closeModal();renderQuestions();showToast('Immutable version committed',`${id} · original remains preserved`);showView('registry');}
    function saveInvention(){const title=$('i-title').value.trim(),mechanism=$('i-mechanism').value.trim();if(!title||!mechanism)return showToast('Title and mechanism are required');const id=`INV-2026-${String(29+state.inventions.length-12).padStart(6,'0')}`;state.inventions.push({id,family:$('i-family').value,code:$('i-code').value.trim()||'NEW',title,status:'Hypothesis',posture:$('i-posture').value,updated:'05 Sep 2026',gate:'Add embodiment and test evidence',mechanism,evidence:[],contributors:1});state.contributions.push({person:$('i-contribution').value.split('·')[0].trim()||'Andre',role:'Relationship / mechanism',detail:$('i-contribution').value.trim()||'Initial human conception recorded at capture.',date:'05 Sep 2026'});persist();closeModal();renderCandidates();showToast('Invention record created',`${id} · human contribution captured`);showView('inventions');}
    function saveContribution(id){state.contributions.push({person:$('c-person').value.trim()||'Unnamed human',role:$('c-role').value,detail:$('c-detail').value.trim()||'Contribution attested without additional detail.',date:'05 Sep 2026'});const item=state.inventions.find(candidate=>candidate.id===id);if(item)item.contributors+=1;persist();closeModal();showToast('Contribution attested',`${id} · AI remains tool-only`);openModal('detail',id);}
    function runPreflight(){const artifact=$('artifact-name').value.trim()||'untitled-artifact · v1.0',linked=$('linked-invention').value,nda=$('nda').value,item=state.inventions.find(candidate=>candidate.id===linked);let decision='GREEN · ALLOWED',tone='green',rationale='No linked unfiled invention or unresolved authority was detected.';if(item&&item.status!=='Filed'){decision='HOLD-FILE-FIRST';tone='red';rationale=`${item.id} is not verified filed. Remove the mechanism, file first, or obtain a counsel decision.`;}else if(item&&nda==='missing'){decision='AMBER · COUNSEL REVIEW';tone='amber';rationale='The linked candidate is filed, but audience confidentiality is not verified.';}const record={id:`DISC-2026-${String(32+state.disclosures.length).padStart(6,'0')}`,artifact,channel:$('channel').value,decision,classification:$('classification').value,linked:linked||'',hash:'sha256: pending-final-artifact',date:'05 Sep 2026'};$('decision-output').innerHTML=`<div class="decision ${tone}"><strong>${esc(decision)}</strong><p>${esc(rationale)}</p><div class="decision-meta"><span class="pill">${esc(record.id)}</span><span class="pill">${esc(record.classification)}</span><span class="pill">${esc(record.hash)}</span></div></div>`;state.disclosures.unshift(record);persist();renderDisclosures();showToast('Preflight recorded',`${record.id} · ${decision}`);}
    renderQuestions();renderCandidates();renderDisclosures();renderCalendar();
  </script>
</body>
</html>
